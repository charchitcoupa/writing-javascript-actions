const core = require("@actions/core");

async function run() {
  try {
    const issueTitle, jokeBody, token and octokit;
    issues.create();
  }
  catch {
    console.log("something went wrong");
    core.setFailed;
  }    
}

run();
